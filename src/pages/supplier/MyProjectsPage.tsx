import {Button,Progress,Space} from 'antd';
import {ColumnsType} from 'antd/es/table';
import {DataTable} from '../../components/DataTable';import {PageHeader} from '../../components/PageHeader';import {StatusTag} from '../../components/StatusTag';import {applications} from '../../mocks/data';import {BidApplication} from '../../types';
export function MyProjectsPage(){const columns:ColumnsType<BidApplication>=[{title:'项目/标段',render:(_,r)=><><strong>{r.projectName}</strong><br/><small>{r.lotName}</small></>},{title:'报名',dataIndex:'registrationStatus',render:v=><StatusTag status={v}/>},{title:'标书费',dataIndex:'feeStatus',render:v=><StatusTag status={v}/>},{title:'保证金',dataIndex:'depositStatus',render:v=><StatusTag status={v}/>},{title:'投标文件',dataIndex:'bidFileStatus',render:v=><StatusTag status={v}/>},{title:'开标时间',dataIndex:'openingTime'},{title:'当前节点',dataIndex:'currentNode'},{title:'流程完成度',render:(_,r)=><Progress percent={r.id==='a1'?55:18} size="small"/>},{title:'下一步操作',fixed:'right',render:(_,r)=><Space><Button type={r.nextAction==='等待审核'?'default':'primary'} disabled={r.nextAction==='等待审核'}>{r.nextAction}</Button></Space>}];return <><PageHeader title="我参与的项目" description="按项目跟踪报名、缴费、保证金、投标文件和开标待办"/><DataTable dataSource={applications} columns={columns} scroll={{x:1300}}/></>}
